# GROMACS Scripts
For already prepared pipeline for simulations (its general, but it was made for simulating collagen fibrils, so the user might need to change som ethings if wanting to do something else). Its all contained in the folder `prep-pipeline`.

The other folders are both archive and deprecated methods, and the shell files are examples of hat you would get using the prep-pipeline, plus another useful commands for the analysis section.
